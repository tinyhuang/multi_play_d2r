<Window x:Class="D2RMultiPlay.Wpf.MonitorLayoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Game Window Layout" Height="560" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        Foreground="{ui:ThemeResource ApplicationForegroundBrush}">

    <DockPanel LastChildFill="True" Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Auto Grid" Click="BtnAutoGrid_Click" MinWidth="120" Margin="0,0,8,0"/>
            <Button Content="Refresh Monitors" Click="BtnRefreshMonitors_Click" MinWidth="140"/>
        </StackPanel>

        <DataGrid x:Name="GridAccounts" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="90" IsReadOnly="True"/>
                <DataGridTextColumn Header="Monitor" Binding="{Binding Layout.MonitorId}" Width="*"/>
                <DataGridTextColumn Header="X" Binding="{Binding Layout.X}" Width="80"/>
                <DataGridTextColumn Header="Y" Binding="{Binding Layout.Y}" Width="80"/>
                <DataGridTextColumn Header="W" Binding="{Binding Layout.W}" Width="80"/>
                <DataGridTextColumn Header="H" Binding="{Binding Layout.H}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" MinWidth="88" Margin="0,0,8,0"/>
            <Button Content="OK" Click="BtnOk_Click" MinWidth="88"/>
        </StackPanel>
    </DockPanel>
</Window>
